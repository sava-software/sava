// Local convention plugins shared by this repo's modules. Kept out of sava-build
// deliberately: these conventions reference files that only exist here (AGENTS.md),
// and sava-build changes need a publish before other repos see them.
rootProject.name = "plugins"
